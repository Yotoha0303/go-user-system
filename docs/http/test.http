@baseUrl = http://127.0.0.1:8082
@username = "test_name"
@password = "test-password-123"
@newPassword = "test-password-456"
@nickname = "test_nickname"

### 0. 服务器健康检查
GET {{baseUrl}}/ping

### 0.1 应用存活检查
GET {{baseUrl}}/livez

### 0.2 服务就绪检查
GET {{baseUrl}}/readyz

### 0.3 Swagger JSON
GET {{baseUrl}}/swagger/doc.json

### 1. 用户注册
POST {{baseUrl}}/api/v1/auth/register
Content-Type: application/json

{
  "username": {{username}},
  "password": {{password}}
}

### 2. 用户登录
# 保存响应里的 access_token；refresh_token 由 HttpOnly Cookie 自动维护。
POST {{baseUrl}}/api/v1/auth/login
Content-Type: application/json

{
  "username": {{username}},
  "password": {{password}}
}

### 3. 刷新双 Token
# 浏览器客户端无需请求体；下面的 refresh_token 请求体仅用于非浏览器客户端。
POST {{baseUrl}}/api/v1/auth/refresh
Content-Type: application/json

{
  "refresh_token": "<refresh_token>"
}

### 4. 访问受保护接口
GET {{baseUrl}}/api/v1/users/me
Authorization: Bearer <access_token>

### 5. 更改用户昵称
PUT {{baseUrl}}/api/v1/users/me/profile
Content-Type: application/json
Authorization: Bearer <access_token>

{
  "nickname": {{nickname}}
}

### 5.1 查询当前用户角色和权限
GET {{baseUrl}}/api/v1/users/me/authorization
Authorization: Bearer <access_token>

### 6. 修改用户密码
# 成功后会吊销该用户所有 refresh_token，需要重新登录。
PATCH {{baseUrl}}/api/v1/users/me/update/password
Content-Type: application/json
Authorization: Bearer <access_token>

{
  "old_password": {{password}},
  "new_password": {{newPassword}}
}

### 7. 退出登录
# 浏览器客户端无需请求体；下面的 refresh_token 请求体仅用于非浏览器客户端。
POST {{baseUrl}}/api/v1/auth/logout
Content-Type: application/json

{
  "refresh_token": "<refresh_token>"
}

### 8. 管理端：查询角色列表
GET {{baseUrl}}/api/v1/admin/roles
Authorization: Bearer <admin_access_token>

### 9. 管理端：查询权限列表
GET {{baseUrl}}/api/v1/admin/permissions
Authorization: Bearer <admin_access_token>

### 10. 管理端：给用户分配角色
PUT {{baseUrl}}/api/v1/admin/users/1/roles
Content-Type: application/json
Authorization: Bearer <admin_access_token>

{
  "role_codes": ["user", "admin"]
}
