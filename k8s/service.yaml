# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: go-user-system
  namespace: go-user-system
spec:
  selector:
    app: go-user-system
  ports:
  - name: http
    port: 8082
    targetPort: http
    protocol: TCP
  type: ClusterIP
