# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: go-user-system
  namespace: go-user-system
spec:
  selector:
    app: go-user-system
  ports:
  - port: 8082
    targetPort: 8082
    protocol: TCP
  type: ClusterIP