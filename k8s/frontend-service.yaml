apiVersion: v1
kind: Service
metadata:
  name: go-user-system-frontend
  namespace: go-user-system
spec:
  selector:
    app: go-user-system-frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
